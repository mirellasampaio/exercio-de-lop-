
#include <stdio.h>
#include <locale.h>

int main () { // ponto de partida
    int n; // Declara uma variável inteira
    
    printf("Digite o numero:\n"); // mostra a informação e os dados 
    scanf("%d", &n); //Para armazenar á variavel
    
    if("nume % 2 == 0") // ele verifica as informações 
    {
        printf("é Par!:\n"); // mostra as informações e dados 
    }
    else // senão 
    {
        printf("É impar!:\n"); // mostra as informações e dados 
    }
    
    return 0; // encerramento do programa 
}















